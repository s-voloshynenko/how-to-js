'use strict';

module.exports = {
  header: '1. Program life cycle in the JS',
  content: 'Program’s life cycle in JavaScript is based on such things like - thread, stack, heap and queue. Note that engine providers like Google (V8), Mozilla (SpiderMonkey), Microsoft (Chakra) are responsible for realization of the most of these things. The first thing that you should now - our code executes in the single thread. There are no any possibility to launch application in the separate thread. Probably you have heard about WebWorkers but it’s related to JS in the browser that is far enought from JS core. Let’s have a look at the simple code example, which creates magic number: '
};
